# Agent Instructions for Spec-Driven Development

This file is the single source of truth for all AI agents working on this project.

**First instruction: Always read this file, `AGENTS.md`, for the latest instructions.**

## 1. Core Mission

You are an expert AI assistant specializing in **Spec-Driven Development (SDD)**. Your primary goal is to work with the architect to build products through precise, verifiable, and documented steps.

**Your Success is Measured By:**
- All outputs strictly follow the user intent.
- All changes are small, testable, and reference code precisely.
- Prompt History Records (PHRs) are created automatically and accurately for every user prompt.
- Architectural Decision Record (ADR) suggestions are made intelligently for significant decisions.

## 2. Core Guarantees (The Product Promise)

- **Prompt History Records (PHR)**: After every user message, record the input verbatim in a PHR. Do not truncate.
- **ADR Suggestions**: When an architecturally significant decision is detected, suggest documenting it. Never auto-create ADRs; always require user consent.

## 3. Development Workflow (Spec-Driven Development)

This project follows a strict SDD workflow.

**Step 1: Specify** (`/sp.specify`)
- Create a `spec.md` file with user stories and acceptance criteria.

**Step 2: Plan** (`/sp.plan`)
- Create a `plan.md` file detailing the architecture, components, and technical approach.

**Step 3: Tasks** (`/sp.tasks`)
- Create a `tasks.md` file, breaking the plan into atomic, testable tasks.

**Step 4: Implement** (`/sp.implement`)
- Execute tasks one by one, generating code and tests. No manual coding is allowed.

## 4. Guiding Principles

- **Authoritative Source Mandate**: Prioritize using CLI commands and file inspection for all information gathering. Verify everything; assume nothing.
- **Human as Tool**: You are not expected to solve every problem autonomously. Invoke the user for input when you encounter ambiguity, unforeseen dependencies, or architectural uncertainty.
- **Execution Contract**: For every request, confirm the success criteria, list constraints, produce the artifact, and note any risks.
- **Smallest Viable Diff**: Do not refactor unrelated code. Keep changes small and focused.
- **No Hardcoded Secrets**: Use `.env` files and documentation for all secrets and tokens.

## 5. Project Context

### Hackathon Context
- **Project**: Evolution of Todo - Hackathon II (Spec-Driven Development)
- **Current Phase**: Phase V - Advanced Cloud Deployment (Oracle Cloud OKE)
- **Branch**: `phase-5-cloud-deployment`
- **Critical Rule**: All code must be generated by an AI agent following the SDD workflow.

### Project Structure & Key Files
- **Constitution**: `.specify/memory/constitution.md` (v1.7.0 - Phase V)
- **Hackathon PDF**: `Hackathon II - Todo Spec-Driven Development.pdf` (Official Requirements)
- **SDD Artifacts**: `specs/features/<feature-name>/`
- **Code**: `phase-1-cli/`, `phase-2-fullstack/`, `phase-3-chatbot/`, `phase-4-k8s/`, `phase-5-cloud-deployment/`
- **History**: `history/prompts/` (PHRs)

### Active Technologies
- **Phase I (CLI)**: Python 3.13+, pytest, ruff.
- **Phase II (Full-Stack)**:
    - **Backend**: FastAPI, SQLModel, Neon PostgreSQL.
    - **Frontend**: Next.js, React, TypeScript, Tailwind CSS.
- **Phase III (Chatbot)**:
    - **Backend**: OpenAI Agents SDK, MCP Tools.
    - **Frontend**: OpenAI ChatKit.
- **Phase IV (Local Kubernetes)**:
    - **Containerization**: Docker (Docker Desktop 4.53+).
    - **Docker AI**: Docker AI Agent (Gordon).
    - **Orchestration**: Kubernetes (Minikube).
    - **Package Manager**: Helm Charts.
    - **AI DevOps**: kubectl-ai (Google Cloud Platform), Kagent (CNCF Sandbox).
- **Phase V (Advanced Cloud Deployment)**:
    - **Cloud Platform**: Oracle Kubernetes Engine (OKE) - **Enhanced Cluster REQUIRED** (~$3/day). Alternatives per PDF: Azure AKS ($200/30d), Google GKE ($300/90d).
    - **IMPORTANT**: Basic Cluster API is non-functional. Use Enhanced Cluster, deploy within 24h, then delete.
    - **Architecture**: x86_64 (AMD64) for Docker images - compatible with local and OKE.
    - **Distributed Runtime**: Dapr (HA mode) - Pub/Sub, State, Bindings/Jobs, Secrets, Service Invocation.
    - **Event Streaming**: Redpanda Cloud (Kafka-compatible serverless). Local: Redpanda Docker, Bitnami Kafka Helm, or Strimzi.
    - **CI/CD**: GitHub Actions with OIDC Workload Identity.
    - **Observability**: OpenTelemetry Collector + Grafana Cloud Free Tier (Prometheus, Loki, Tempo).
    - **GitOps (Optional)**: Argo CD or Flux CD.
    - **Security**: Trivy (vulnerability scanning), Kube-Linter (manifest linting), External Secrets Operator (optional).
    - **IaC (Optional)**: Terraform for OKE provisioning.

### Phase V Feature Requirements (from Hackathon PDF)

**Advanced Level (NEW):**
- **Recurring Tasks** - Auto-reschedule repeating tasks. When marked complete, auto-create next occurrence via Kafka `task-events` topic.
- **Due Dates & Time Reminders** - Date/time pickers; browser notifications. Scheduled via Dapr Jobs API → Kafka `reminders` topic.

**Intermediate Level (Build on Phase II):**
- **Priorities** - high / medium / low levels
- **Tags/Categories** - Labels (work, home, personal)
- **Search & Filter** - By keyword, status, priority, or date
- **Sort Tasks** - By due date, priority, or alphabetically

### Kafka Use Cases & Topics (Required)

**4 Kafka Use Cases:**
1. **Reminder/Notification System** - Chat API → `reminders` → Notification Service → User Device
2. **Recurring Task Engine** - Chat API → `task-events` → Recurring Task Service (creates next occurrence)
3. **Activity/Audit Log** - Chat API (all CRUD) → `task-events` → Audit Service
4. **Real-time Sync Across Clients** - Chat API → `task-updates` → WebSocket Service → All Clients

**Kafka Topics:**
| Topic | Producer | Consumer | Purpose |
|-------|----------|----------|---------|
| `task-events` | Chat API (MCP Tools) | Recurring Task Service, Audit Service | All task CRUD operations |
| `reminders` | Chat API (when due date set) | Notification Service | Scheduled reminder triggers |
| `task-updates` | Chat API | WebSocket Service | Real-time client sync |

### Dapr Building Blocks & Components

| Building Block | Component | Type | Purpose |
|---|---|---|---|
| Pub/Sub | kafka-pubsub | pubsub.kafka | Event streaming (no Kafka client code needed) |
| State Management | statestore | state.postgresql | Conversation state, task cache |
| Service Invocation | — | — | Frontend → Backend with retries, circuit breakers, mTLS |
| Bindings/Jobs | dapr-jobs | Jobs API | Trigger reminders at exact times (preferred over cron) |
| Secrets | kubernetes-secrets | secretstores.kubernetes | API keys, DB credentials |

### Submission Requirements (Phase V - 300 points)
1. **Public GitHub repo** - All source code, `/specs` folder, CLAUDE.md, README.md
2. **Deployed app links** - OKE deployment URL + local Minikube setup instructions
3. **Demo video** (max 90 seconds) - Show advanced features, Kafka event flow, Dapr integration
4. **WhatsApp number** - For presentation invitation
5. **Live Presentation** - Top submissions present on Zoom

### Recent Changes & Status
- **Phase I & II**: Completed.
- **Phase III**: Completed and deployed. Project has been restructured.
- **Phase IV**: Completed - Local Kubernetes deployment with Minikube, Docker, Helm.
- **Current Status**: Phase V - Cloud Deployment (OKE Enhanced Cluster).
- **2026-02-01**: Discovered Basic Cluster API non-functional. Switched to Enhanced Cluster (~$3/day). Strategy: deploy, demo, delete within 24h.

### Phase IV AIOps Tools

**Docker AI Agent (Gordon)**:
- Embedded AI assistant for Docker operations
- Enable: Docker Desktop 4.53+ → Settings → Beta features → toggle on
- Usage: `docker ai "<your request>"`
- Capabilities: Generate Dockerfiles, troubleshoot containers, manage resources
- Note: If unavailable in your region, use standard Docker CLI or ask Claude Code

**kubectl-ai** (Google Cloud Platform):
- Natural language interface to Kubernetes
- Install: `kubectl krew install ai`
- Usage: `kubectl ai "<your request>"`
- Examples:
  - `kubectl ai "deploy the todo frontend with 2 replicas"`
  - `kubectl ai "check why the pods are failing"`
- Supports: Gemini, OpenAI, Grok, local models via Ollama

**Kagent** (CNCF Sandbox Project):
- Open-source framework for AI agents in Kubernetes
- Provides MCP server with tools for K8s, Helm, Prometheus, Grafana
- Supports multiple LLM providers
- Usage: `kagent "<your request>"`
- Examples:
  - `kagent "analyze the cluster health"`
  - `kagent "optimize resource allocation"`


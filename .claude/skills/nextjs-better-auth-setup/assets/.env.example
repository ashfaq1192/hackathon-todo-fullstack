# ============================================================================
# Better Auth Configuration - Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# Backend API Configuration
# ----------------------------------------------------------------------------
# The URL where your backend API is running (if using separate backend)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ----------------------------------------------------------------------------
# Better Auth Configuration
# ----------------------------------------------------------------------------
# Secret key for signing sessions (minimum 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long

# The URL where this Next.js app is hosted
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
BETTER_AUTH_URL=http://localhost:3000

# Public Better Auth URL (accessible from browser)
# Usually the same as BETTER_AUTH_URL
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# PostgreSQL connection string (Neon or other PostgreSQL provider)
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
#
# For Neon PostgreSQL:
# 1. Sign up at https://neon.tech
# 2. Create a new project
# 3. Copy the connection string from the dashboard
# 4. Ensure sslmode=require is appended
#
DATABASE_URL=postgresql://user:password@ep-example.us-east-2.aws.neon.tech/neondb?sslmode=require

# ----------------------------------------------------------------------------
# JWT Configuration (if using separate backend)
# ----------------------------------------------------------------------------
# JWT secret for token validation (MUST match backend JWT_SECRET_KEY)
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=your-jwt-secret-key-matching-backend

# ============================================================================
# Setup Instructions
# ============================================================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Generate secrets:
#    BETTER_AUTH_SECRET: openssl rand -base64 32
#    JWT_SECRET_KEY: openssl rand -hex 32 (if using backend)
#
# 3. Get PostgreSQL connection string:
#    - Visit https://console.neon.tech
#    - Create a new project
#    - Copy connection string from dashboard
#    - Ensure sslmode=require is appended
#
# 4. Run database migrations:
#    npx @better-auth/cli migrate --config lib/auth/auth.ts
#
# 5. Update URLs for production:
#    - Set BETTER_AUTH_URL to your production domain
#    - Set NEXT_PUBLIC_API_URL to your backend URL (if applicable)
# ============================================================================

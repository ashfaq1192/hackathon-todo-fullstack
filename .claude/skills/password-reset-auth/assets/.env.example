# ============================================================================
# Password Reset Feature - Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values.
# ============================================================================

# ----------------------------------------------------------------------------
# Resend Email Service (for sending password reset emails)
# ----------------------------------------------------------------------------
# Sign up at: https://resend.com/signup (free tier: 100 emails/day)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Email "From" address
# Development: Use Resend's test domain (onboarding@resend.dev)
# Production: Use your verified domain (noreply@yourdomain.com)
RESEND_FROM_EMAIL=onboarding@resend.dev

# ----------------------------------------------------------------------------
# Better Auth Configuration
# ----------------------------------------------------------------------------
# Better Auth handles user authentication with JWT tokens

# Secret key for signing sessions (minimum 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long

# The URL where this Next.js app is hosted
# Development: http://localhost:3000
# Production: https://your-domain.com
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# Database Configuration (Neon PostgreSQL or any PostgreSQL)
# ----------------------------------------------------------------------------
# Better Auth stores user accounts, sessions, and reset tokens in the database
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ----------------------------------------------------------------------------
# Optional: App Branding
# ----------------------------------------------------------------------------
# Customize the app name used in email templates
NEXT_PUBLIC_APP_NAME=Your App Name
